# Tech Stack

<!-- GENERATE: CODEBASE_CONTEXT
Description: Stack details — runtime, languages, frameworks.
Required manifest fields: stack.primary, stack.detected, stack.runtime
Invariant rules:
- Config files live only inside Agentbase
- A `.claude/` directory is not created inside Codebase
- Git runs only in Codebase
-->

## Test

<!-- GENERATE: VERIFICATION_COMMANDS
Description: Test command for each subproject.
Required manifest fields: project.subprojects, stack.test_commands
-->

## File Extensions

<!-- GENERATE: FILE_EXTENSIONS
Description: Source file extensions used in the project.
Required manifest fields: stack.primary, stack.detected
-->

## Security Patterns

<!-- GENERATE: SECURITY_PATTERNS
Description: Security check patterns by stack.
Required manifest fields: stack.primary, stack.detected, stack.orm
-->
